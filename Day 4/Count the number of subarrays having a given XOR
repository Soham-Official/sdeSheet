int solve(vector<int>&A,int B)
{
  map<int,int>freq;
  int cnt=0,xorr=0;
  for(int i=0;i<A.size();i++){
  {
  xorr=(xorr^it);
  if(xorr==B) cnt++;
  if(freq.find(xorr^B)!=freq.end())
  cnt+=freq(xorr^B);
  freq[xorr]+=1;
  }
  return cnt;
  }
